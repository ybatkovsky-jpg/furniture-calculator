# Замечания по тестированию — furniture-calculator

**Дата:** 2026-09-06 · **Статус сессии:** V2-валидация 12/12 эталонов, суит **251/251** зелёных (все модули расчётного ядра покрыты), `check_phases.py` 59/59.

---

## 1. Проблемы, с которыми столкнулись

### P1. Переполнение контекста сабагента (потеря работы)
- **Что:** делегированный сабагент (задача: тесты для 6 модулей — glass_calc, edge_calc, sheet_calc, fastener_calc, hardware_calc, quantity_calc) исчерпал контекст до завершения. Артефактов не осталось: новых файлов в `tests/` нет, суит остался на 85 тестах.
- **Причина:** слишком широкая задача для одного агента (чтение 6 модулей + написание тестов + отладка).
- **Митигация:** дробить делегирование на узкие задачи; в промпт сабагенту добавлять «пиши файл сразу после каждого модуля, не копируй исходники целиком».
- **Обновление 2026-07-23 (повтор):** задача «2 модуля» (glass_calc + edge_calc) — **успех**, +20 тестов (105/105 зелёные). Задача «3 модуля» (sheet + fastener + quantity_calc, 935 строк исходников) — **снова переполнение**, артефактов нет. Вывод: надёжный лимит ≈ **1 модуль на сабагента** (до ~200 строк исходника); большие модули (quantity_calc 681 стр.) — отдельно, с запретом читать смежные файлы.

### P2. Ложный флаг валидации на стр. 0008 (кровать)
- **Что:** `v2_validate_all.py` помечает страницу ⚠️, если `facades == []`. Стр. 0008 — **кровать в детской** (визуально подтверждено: каркас, ламели, мягкие панели, размерная линия 2200 мм в Вид-1 и Виде-сверху; дверцей/фасадов на чертеже нет). Модель ответила верно, но сводка показала «Модули найдены: 11/12».
- **Причина:** эвристика `has_modules = len(facades) > 0` не учитывает тип мебели — кровати/столы/диваны легитимно без фасадов.
- **Итог:** фактическая точность zone_type и габаритов — **12/12 (100%)**, а не «11/12 с модулями».

### P3. Последовательные API-вызовы в массовом прогоне
- **Что:** `v2_validate_all.py` обрабатывает страницы строго по одной: 12 страниц ≈ 30 минут wall-time (~2–3 мин на страницу, видно по таймстампам JSON).
- **Причина:** цикл `for ... await call_v2(...)` без параллелизма.

### P4. Confidence не дискриминирует
- **Что:** модель отвечает `"confidence": "high"` даже на сложных страницах (кровать, ванная со стеклом). Поле бесполезно для маршрутизации/флажирования слабых результатов.
- **Причина:** в промпте нет явной рубрики критериев; модель по умолчанию ставит high.

### P5. Разрыв между скриптом валидации и пайплайном
- **Что:** `v2_validate_all.py` зовёт RouterAI `qwen/qwen3-vl-235b-a22b-thinking`, а основной пайплайн (commit 58f6e54) — локальную Qwen3.8 через unsloth-studio. Результаты двух маршрутов могут расходиться, но сравнения нет.
- **Причина:** скрипт написан до перехода на локальную модель.

### P6. Пропуски покрытия тестами (не баг, но риск)
- ~~Не покрыты юнит-тестами: `glass_calc`, `edge_calc`, …~~ → **2026-07-23:** glass_calc + edge_calc покрыты (+20 тестов, 105/105 зелёные); sheet/fastener/quantity — в работе.
- **Обновление 2026-09-06:** `sheet_calc` и `fastener_calc` покрыты (test_sheet_calc.py, test_fastener_calc.py) → суит **136/136 зелёные**. Осталось: `quantity_calc` (681 строка — отдельно, 1 модуль на сабагента).
- **Обновление 2026-09-06 (вечер):** `quantity_calc` покрыт — **tests/test_quantity_calc.py, +73 теста** (detect_material_properties, calculate_quantities, fill_template_for_room, MaterialQuantities) → суит **209/209 зелёные**. Написаны напрямую в сессии (опыт P1: 1 модуль на делегирование; модуль 808 строк читался целиком).
- Остались без покрытия: `template_filler` (I/O), `pdf_parser` (I/O).

### P7. Найденные кверки расчётного ядра (не исправлены, зафиксированы тестами)
Найдены при написании тестов glass/edge (сабагент e411b0bb):
1. **`edge_calc.py:167-169` vs `:99`** — несогласованность: `calculate_edge_for_modules` пропускает нулевые/отрицательные длины, а `calculate_edge_for_detail` — нет (нулевой элемент даёт `{"0.8": 0.0}`). Закреплено регрессионным тестом.
2. **`glass_calc.py:70-71`** — MORU считается плоскими 1 листом на элемент независимо от размера (0×0 стоит 4250 ₽; >6,6 м² — всё равно 1 лист). В коде помечено «упрощённо» — задокументированное поведение.
3. **`glass_calc.py:80-83`** — `glass_type="tinting"` тарифицируется как 2000 ₽/м² (по площади), а не как фиксированный +2000 ₽, т.к. `"tinting"` лежит внутри `GLASS_PRICES`.
4. **`glass_calc.py:66,77`** — нет защиты от отрицательных размеров → отрицательная стоимость (тест `test_negative_dimensions_produce_negative_cost`).

Найдены при написании тестов quantity_calc (2026-09-06, тесты в `tests/test_quantity_calc.py`):
5. **`quantity_calc.py` — кромка МДФ 2мм для высоких фасадов теряется.** Фасад МДФ >2000 мм (EMDIWAY/EVOGLOSS и т.п.) копит периметр в `q.edge_2_m`, но финальное распределение безусловно перезаписывает `edge_2_m = ceil(edge_premium_m) = 0` (premium нигде не накапливается). В смету высокий МДФ-фасад попадает БЕЗ кромки 2мм, и в 1мм она тоже не попадает (ветка `>2000`). Тест `test_mdf_tall_facade_2mm_edge_is_lost`. Это потеря реального материала в смете — кандидат на фикс.
6. **`quantity_calc.py` — `detect_material_properties([])` не возвращает ключ `mdf_needs_edge`** (ранний return до установки). Безопасно из-за `.get()`, но форма результата зависит от пустоты входа. Тест `test_props_empty`.
7. **`quantity_calc.py` — высота в Gola берётся БЕЗ припуска на пропил** (0.82 м для модуля 820, не 0.824) — мелочь, согласовано между вертикальными профилями и периметром? Нет: периметры кромки считаются с +4мм, Gola — без. Расхождение ~0.5% на профиль. Зафиксировано тестами `test_gola_vertical_*`.

---

## 2. Что улучшить для ТОЧНОСТИ

| # | Улучшение | Где | Ожидаемый эффект |
|---|-----------|-----|------------------|
| A1 | **Зона-зависимые ожидания валидации**: правила «какие поля обязательны» per `zone_type` (кровать/стол → фасады могут быть пустыми; signal — total_width + notes) | `v2_validate_all.py` / `furniture_defaults.py` | Убрать ложные ⚠️, честная статистика |
| A2 | **Кросс-валидация OCR↔Vision** (уже в roadmap «Дальнее»): размерные линии из OCR как ground truth для `total_width_mm` и ширин фасадов; расхождения — флаги в листе «Контроль качества» | `full_pipeline.py`, `template_filler.py` | На стр. 0008 оба источника дали 2200 — механизм работает; ловит «выдуманные» размеры раньше |
| A3 | **Рубрика confidence в промпте**: high = все размеры прочитаны с размерных линий; medium = частично оценены; low = только эвристика. Либо считать её кодом (OCR↔Vision match) | `UNIFIED_PROMPT_V2` / постобработка | Поле становится пригодным для авто-флажирования |
| A4 | **Few-shot для не-кухонь** (шкаф-купе, гардеробная, кровать) — уже в roadmap «Среднесрочное» | `UNIFIED_PROMPT_V2` | Улучшение zone_type/facades на 0008-типах страниц |
| A5 | **Доп. тесты ядра** (P6): glass/edge/sheet/fastener + quantity_calc — дробными делегациями по 1–2 модуля | `tests/` | Регрессии в расчётах материалов ловятся до Excel. Статус 2026-09-06: glass+edge+sheet+fastener готовы (136/136), quantity_calc — в работе |

## 3. Что улучшить для СКОРОСТИ

| # | Улучшение | Где | Ожидаемый эффект |
|---|-----------|-----|------------------|
| S1 | **Параллельные вызовы**: `asyncio.Semaphore(3–4)` + `gather` в массовом прогоне | `v2_validate_all.py` | 12 страниц: ~30 мин → ~6–8 мин |
| S2 | **Кэш результатов** keyed by `(prompt_version, image_sha256)`: повторный прогон после правки промпта не пересылает уже обработанные; новые — только при смене версии | `v2_validate_all.py` / общий слой | Экономия API-вызовов и времени на итерациях промптов |
| S3 | **A/B локальная vs облачная** Qwen3.8: прогнать один альбом через unsloth-studio и RouterAI, сравнить точность/время; зафиксировать победителя как дефолт | `scripts/dev/compare_prompts_ab.py` (расширить) | Скорость + стоимость без потери качества (если локальная не уступает) |
| S4 | **Двухпроходный режим**: быстрый проход на 1024px для скрининга zone_type/габаритов → детальный проход только на флажированные страницы | `image_analyzer.py` / скрипты | ~2× быстрее на «лёгких» страницах, точность на сложных не теряется |

---

## 4. Итоги текущего прогона (12/12 эталонов)

| Метрика | Значение |
|---------|----------|
| zone_type валиден | **12/12 (100%)** |
| total_width_mm в диапазоне | **12/12 (100%)** |
| Модули (facades>0) | 11/12 — стр. 0008 ложный флаг (кровать, см. P2) → фактически **12/12 корректных ответов** |
| Ящики найдены | 7/12 |
| Стекло найдено | 5/12 |
| Распределение зон | Ванная×3, Кухня×2, Гостиная×2, Спальня×2, Детская×2, Прихожая×1 — все валидные |
| Юнит-тесты | **85/85 passed** (0.97 c) |

Результаты: `output/v2_validation/*.json` + `_summary.json`.

---

## 5. Замер точности по альбому «Рокоссовского 59-79» (2026-09-06)

**Ограничение сессии:** у текущей модели нет vision-входа (`read_image` недоступен) —
«сверка глазами» с чертежами невозможна. Ниже — **кросс-маршрутный замер**:
сравнение двух независимых прогонов одного промпта UNIFIED_PROMPT_V2 на тех же 12
картинках в один день (06.09):
- **Маршрут A (локальный, полный пайплайн, 11:26)** — `scripts/process_images.py`,
  локальная Qwen3.8 (`orcarouter/Qwen3.8-27B-Uncensored-GGUF`), лог `temp/full_run_12p.log`.
- **Маршрут B (облачный, валидация, 12:36)** — `scripts/dev/v2_validate_all.py`,
  RouterAI `qwen/qwen3-vl-235b-a22b-thinking`, JSON в `output/v2_validation/`.

Расхождения маршрутов — кандидаты на ошибки распознавания (нужна ручная/vision-проверка).

| Стр. | Зона A (лок) | Зона B (обл) | total A | total B | Фасады A→B | Ящики A | Стекло B | Комментарий |
|------|--------------|--------------|---------|---------|------------|---------|----------|-------------|
| 0001 | Прихожая | Прихожая | **1650** | **1100** | 2→3 | 1 | [2] | ⚠️ total расходится на 550 мм |
| 0002 | Кухня | Кухня | 4860 | 4860 | 21→10 | 0 | [7,10] | ⚠️ 21 дверь (до группировки) vs 10; авто-коррекция penal 1356→1000 |
| 0003 | Кухня | Кухня | 1750 | 1750 | 8→6 | 3 | — | ⚠️ 8 vs 6 фасадов; авто-коррекция 154→250 ×2 |
| 0004 | Гостиная | Гостиная | 1830 | 1830 | 1→4 | 1 | — | ⚠️ 1 «корнер 1000» vs 4 фасада; авто-коррекция 1830→1000 |
| 0005 | Гостиная | Гостиная | 3080 | 3030 | 3→3 | 0 | [1,2,3] | ✅ согласовано (±50) |
| 0006 | Спальня | Спальня | 3200 | 3200 | 4→4 | 2 | — | ✅ число согласовано |
| 0007 | Спальня | Спальня | 1600 | 1600 | 2→3 | 2 | — | ⚠️ 2 penal vs 3 фасада |
| 0008 | Детская | Детская | 2200 | 2200 | **2→0** | 1 | — | ⚠️ локальный прогон дал corner 1000+upper 1131; облако и июльская ревизия — кровать без фасадов |
| 0009 | Детская | Детская | 2000 | 2000 | 4→4 | 1 | — | ✅ согласовано |
| 0010 | Ванная | Ванная | 1260 | 1260 | 4→4 | 0 | [1,2] | ✅ согласовано |
| 0011 | **Кухня** | **Ванная** | **2581** | **1581** | 4→7 | 2 | [4..7] | ⚠️ зона И total расходятся; 2 corner 1000 + 2 penal 1161 vs 7 фасадов; авто-коррекция 1290→1000 ×2 |
| 0012 | Ванная | Ванная | 800 | 800 | 2→4 | 1 | — | ⚠️ corner+penal vs 4 фасада |

**Итоги:**
- Зоны согласованы **11/12** (расхождение: стр. 0011 — Кухня vs Ванная).
- Габарит согласован **10/12** (крупные расхождения: 0001 = 1650 vs 1100, 0011 = 2581 vs 1581).
- Число фасадов/модулей согласовано **4/12** (0005, 0006, 0009, 0010); остальные 8 — модель
  дробит/группирует по-разному. Это согласуется с P5 (маршруты расходятся) — качество модели,
  не регрессия пайплайна, но именно эти страницы надо проверять оператором.
- Авто-коррекции ширины фасадов (масштабный пост-процессинг) сработали на **5 из 12** страниц:
  0002, 0003, 0004, 0008, 0011 — все они же и в списке расхождений выше.

**Требуют ручной/vision-сверки с чертежами (приоритет):** 0011 (зона+габарит), 0001 (габарит),
0008 (кровать vs модули), 0004 (1 корнер vs 4 фасада). Средний QC-score листа «Контроль качества»
в полном прогоне 06.09 — 0% — см. вопрос Q1 ниже.

### Решённый вопрос
- **Q1 (закрыт 2026-09-06).** Лист «Контроль качества» после `process_images.py` 06.09:
  «12 помещений, средний score=0%». Причина: images-скрипты создавали `RoomSpec` без вызова
  `_calculate_quality()` (он был только в PDF-пути `FullPipeline`) → `quality_score` оставался 0.0.
  **Фикс:** `process_images.py` и `process_all_resumable.py` теперь вызывают `_calculate_quality(room)`
  — лист КК получает реальные скоры и флаги (confidence, однотипные модули, нулевые размеры и т.д.).

---

## 6. Сессия 2026-09-06 (вечер): сверка оператором + промпт v2.2 + фиксы ядра (суит 209 → 251)

### 6.1 Операторская vision-сверка спорных страниц (§5 → закрыто)
Сверка через локальную vision-модель Qwen3.8 (`scripts/dev/operator_check.py`,
отчёт `output/v2_validation/operator_check.md`). Вердикты:

| Стр. | Расхождение 06.09 | Вердикт сверки | Прав |
|------|-------------------|----------------|------|
| 0001 | A: 1650/2 фас. · B: 1100/3 фас. | Шкаф, total=**1650**; 1100 — ширина фасадного блока 2×550 | **A** |
| 0004 | A: «корнер 1000» · B: 1830/4 фас. | TV-тумба **1830**, углового модуля НЕТ; фасадов 3 (у B 4) — нужна до-сверка | **B** (габарит) |
| 0008 | A: corner+upper · B: 0 фас. | **Кровать** (каркас, велюровые панели, ламели), дверец 0, 2200 = длина | **B** |
| 0011 | A: Кухня 2581 · B: Ванная 1581 | **Санузел №2** под стиральную машину, total=1581 | **B** |

### 6.2 Промпт v2.2 и валидация 12/12
`prompts/unified.txt` + fallback в `image_analyzer.py`: раздел «МЕБЕЛЬ БЕЗ ДВЕРЦ» (кровать/стол/диван →
`facades:[]` + `total_width_mm` + notes), ПРИМЕР 6 (кровать), рубрика confidence, правило шкафа-купе (створка = элемент).
Прогон `v2_validate_all` (локальный маршрут, параллельность 3): **zone 12/12, total 12/12, ошибок API 0**.
- 0008 → Детская, `facades=[]` ✅ (ложный ⚠️ и выдуманные corner+upper устранены);
- 0004 → Гостиная 1830, ложный «угловой 1000» исчез ✅;
- 0001 → 1650 (верно по сверке; у облака было 1100) ✅;
- **0011 → всё ещё «Кухня 2581» ❌** (ground truth: санузел 1581) — открытый дефект локальной модели
  (облако определяло верно): нужен few-shot санузла с техникой или пост-проверка по notes/габариту;
- стекло на 0010 в локальном прогоне не найдено (у облака [1,2]) — вероятна вариативность модели, флаг на повторную проверку.
- **Баг скрипта (исправлен):** `v2_validate_all.py` при локальном маршруте постил в `LOCAL_LLM_API_URL`
  без `/chat/completions` → HTTP 405 на всех страницах (облачный маршрут работал — URL RouterAI полный).
  Введён `CHAT_URL`. Плюс **S1** параллельность (`Semaphore(3)`+`gather`: ~30 мин → ~5–8 мин) и
  **A1** zone-aware ожидания (`NO_DOOR_ZONES`, поле `facades_optional`, `--summary-only` без API).

### 6.3 Фиксы расчётного ядра
1. **P7-5 (кромка МДФ 2мм)** — реальная потеря материала: высокий МДФ-фасад >2000 мм терял кромку 2мм
   (`edge_2_m` безусловно перезаписывался `ceil(edge_premium_m)=0`). Фикс: `edge_2_m = ceil(premium) + ceil(накопленное)`.
2. **Раздвижные двери**: sliding-зоны (Спальня/Гардеробная/wardrobe, `door_system="sliding"`) больше НЕ получают
   петли и накладные ручки (`is_sliding`). Раздвижная система НЕ учтена — готовой позиции с ценой в прайсе нет (TODO).
3. **Петли «высота×вес» (BLUM/HETTICH)**: `hinges_per_door(+door_weight_kg=None)` — вес опционален (0/None → высотная
   таблица как раньше); пороги >18 кг (<900 мм) и >20 кг (≥900 мм) → +1 петля; вес фасада из габаритов и материала
   (ЛДСП 700 / МДФ 780 / стекло 2500 кг/м³, `estimate_facade_weight_kg`); **FIRMAX не тронут** (только высота);
   parity quantity_calc ↔ hardware_calc сохранён (4 parity-теста).
4. **Null-safe мебель без дверей**: кровать/стол/диван (зоны Детская/Спальня/Гостиная/Прихожая) при `modules==[]`
   больше НЕ уходят в legacy `analyze_drawing` (тот выдумывал из кровати lower_base 2000×1136×1800 с 5 ящиками,
   отбрасываемый валидацией → ложная «ошибка распознавания» и пустой результат без Excel).
   Гейт `is_no_door_zone_page()` в `full_pipeline.py`; `process_images.py` печатает «⏭️ Не корпусная мебель: <зона> —
   расчёт не требуется», при всех-no-door — понятное сообщение, exit 0. Кухни/ванные при пустых modules — как раньше.

### 6.4 Итоги сессии
- **pytest: 251/251 passed** (209 базовых + 42 новых), **check_phases.py: 59/59**, exit 0.
- Смоук стр. 0008: 1 vision-вызов (единый анализ), `analyze_drawing` не вызывался, exit 0.
- Изменённые исходники: `quantity_calc.py`, `hardware_calc.py`, `image_analyzer.py` (fallback-константа),
  `full_pipeline.py`, `scripts/process_images.py`, `scripts/dev/v2_validate_all.py`, `prompts/unified.txt`;
  новые: `tests/test_no_door_gate.py`, `scripts/dev/operator_check.py`, `scripts/dev/prompt_probe.py`.
- Открытые дефекты/TODO: стр. 0011 (санузел vs кухня); раздвижная система не учтена (нет позиции в прайсе);
  12 типов модулей в промпте (таксономия MODULE_TYPES готова, смена схемы JSON — отдельный шаг);
  до-сверка числа фасадов стр. 0004 (3 vs 4).

---

# Памятка агента (важные контекстные факты)

## Терминология пользователя
- **«анслот» / «анслот студио» = Unsloth Studio** — десктопное приложение от Unsloth AI,
  которое умеет НЕ ТОЛЬКО дообучать модели, но и **запускать их локально (inference)**.
  Поддерживает GGUF-модели, включая multimodal/vision (например Qwen-VL и т.п.).
  НЕ спрашивать пользователя повторно, что такое «анслот».

## Локальное окружение пользователя (Windows) — проверено фактами (проверка от текущей сессии)
Диски: C: (система), D: (осн. данные, ~1.8 ТБ), E: (данные), F: (пусто/привод).
- **Unsloth Studio НЕ установлен** (нет папки `~\.unsloth`, нет exe в Program Files/AppData).
- **LM Studio** установлен (`~\.lmstudio\llmster\...`), но **скачанных моделей нет** — только встроенная
  embedding-модель `nomic-embed-text-v1.5-GGUF` внутри бандла.
- **Ollama**: только 3 «облачные» манифеста: `gemma4:31b-cloud`, `glm-5.1:cloud`, `minimax-m3:cloud`
  (тег `-cloud` = не локальные веса).
- **HF hub cache** (`~\.cache\huggingface\hub`): только `models--Systran--faster-whisper-tiny`.
- **Falcon-OCR на машине НЕТ** (ни .gguf, ни папок Falcon/OCR на C:/D:/E:).
- Репозитория **`unsloth/Falcon-OCR` и `unsloth/Falcon-OCR-GGUF` не существует** на HuggingFace.
- Falcon-OCR есть только как `tiiuae/Falcon-OCR` (transformers/safetensors) + community fp8/oQ6/oQ8,
  MLX (`mlx-community/Falcon-OCR-bf16`), ONNX (`ningpp/Falcon-OCR-ONNX`). **GGUF-версии нет** →
  Unsloth Studio (работает с GGUF) запустить Falcon-OCR не может.
- Стандартные места хранения моделей (Windows, `Path.home()` = `C:\Users\Yura`):
  - Unsloth Studio cache: `~\.unsloth\studio\cache` (и `~\.cache\huggingface\hub`)
  - LM Studio: `~\.lmstudio\models` (legacy `~\.cache\lm-studio\models`)
  - Ollama: `~\.ollama\models`

## Проект
- `furniture-calculator` — калькулятор мебели («ПРО МЕБЕЛЬ»).
- **Основная vision-задача: анализ ЧЕРТЕЖЕЙ/картинок МЕБЕЛИ → подсчёт ДСП (панелей/раскроя) и фурнитуры.**
  Это НЕ OCR текста документов (счета/прайсы — НЕ целевой кейс; пользователь это явно поправил).
- Пайплайн: изображение чертежа → VLM по промпту `prompts/unified.txt` возвращает JSON
  (zone_type, total_width_mm, facades[], drawer_indices[], heights_mm, materials…) →
  Python считает ДСП и фурнитуру.
- Входные тестовые изображения: `input_images/Альбом чертежей Рокоссовского-59-79_page-*.jpg`
  (стр. 0008 = кровать в детской ~2200мм, без дверей — эталонный кейс).
- **ДВА пути расчёта ДСП/фурнитуры (расходятся!):**
  1. Чат бота (смета в Telegram) → `calc_engine.calculate_full_cost` + `hardware_calc.py` (петли+ящики, БЕЗ ручек)
     + `sheet_calc.py` (плоская формула листов `ceil(площадь/4.927)`, без экономии смежных стенок).
  2. Excel → `quantity_calc.calculate_quantities` (полный: петли+ручки+ящики+направляющие+кромка+ДСП с запасом
     и экономией смежных стенок). Используется в `calculation_excel.py` и `template_filler.py`.
  Полный и правильный — `quantity_calc`. Чат-смета (calc_engine) НЕ считает ручки и грубее по ДСП.

## Ключи / конфигурация (.env — в .gitignore, не коммитить)
- Все ключи лежат в `.env` (в git не попадает). Добавлять туда, а не в AGENTS.md/README.
- **DeepSeek Vision** (`deepseek-v4-flash-vision-exp`): `DEEPSEEK_API_KEY`, `DEEPSEEK_API_URL`,
  `DEEPSEEK_VISION_MODEL`. Это API-модель (облако), в Unsloth Studio локально не ставится.
- Рядом: `ZAI_API_KEY` (GLM-5V для детекции модулей/OCR), `ROUTERAI_API_KEY` (ансамбль Qwen3-VL),
  `GEMINI_API_KEY`, `LOCAL_LLM_*` (Qwen GGUF @ 192.168.1.133:8888).
- Локальные OCR-модели в Unsloth Studio (GGUF): `unsloth/Qwen3-VL-4B-Instruct-GGUF` (заменяет Falcon-OCR),
  `unsloth/Nanonets-OCR-s-GGUF` (англ.), плюс добавлен `unsloth/Qwen3-VL-4B-Instruct-GGUF`.

## Проверенный факт vision-анализа ЧЕРТЕЖЕЙ (тест: стр. 0008 и 0001 альбома)
- **Стр. 0008 (кровать, эталон): 27B / 4B / 8B / DeepSeek сошлись** — Детская, 2200мм, facades=[].
- **Стр. 0001 (шкаф в прихожей, сложный многоракурсный чертёж):**
  - **27B (`orcarouter/Qwen3.8-27B`) — самый детальный**: 2 фасада penal + drawer_indices=[1],
    высота 2483, ширина 1650, notes раскрывает структуру (левая секция ящики+ниша, правая распашные фасады).
  - **8B (`unsloth/Qwen3-VL-8B-Instruct-GGUF`) — согласована с 27B по главному**: ширина 1650, 2 двери, 1 ящик.
    Различия: зона `lower` вместо `penal`, высоту не дала, drawer idx=2 вместо 1. Для счёта фурнитуры — эквивалентна 27B.
  - **4B (`Qwen3-VL-4B`) — слабая на сложных**: ширина 1100 и facades=[] («дверец нет») — противоречит 27B/8B.
    Только простые листы.
  - **DeepSeek vision — НЕ отдал JSON**: ушёл в рассуждения и упёрся в max_tokens (обрыв). Для этой задачи нет.
- **Вывод:** рабочий контур = локальная VLM. Отдельная OCR-модель (Falcon/Nanonets/DeepSeek) НЕ нужна.
- **Решение пользователя:** **27B — основная** (в `.env` `LOCAL_VISION_MODEL=orcarouter/Qwen3.8-27B-Uncensored-GGUF`),
  **8B (`unsloth/Qwen3-VL-8B-Instruct-GGUF`) — запасная** (ручное переключение в Unsloth Studio).
- **Батч-тест 12 страниц (27B vs 8B):** 27B — 46 дверей / 8B — 15. 8B систематически не видит двери/ящики/стекло
  (на 0003,0006,0007,0009,0010,0011 даёт «дверец нет»), почти не читает высоты, путает зону (0011 «Постирочная»).
  Проверено и с включённым thinking у 8B — тот же результат (не параметр, а модель слабее).
  **8B как fallback для подсчёта фурнитуры НЕ годна** — недосчитает ~2/3 петель/ручек. Держать 27B.
- **Стр. 0002 (угловая кухня) — ИСТИНА от пользователя:** общая длина разворота **4860мм**, фасадов **15**
  (верх 7 + низ 5 + левый пенал 2 + правый пенал 1; модуль с плитой без фасада).
  27B: фасады угадала (15), но ширину УДВОИЛА (9360 = ~2×4860) — сложила плечи угловой кухни в развороте.
- **Промпт поправлен** (prompts/unified.txt + вшитый в image_analyzer.py): добавлено правило про угловую кухню в
  развороте («не суммируй плечи, бери одну линию разворота»). Но повторный прогон всё равно дал 9360 — модель
  нестабильна (в notes теперь правильно: «плечо 3030 + 4860 + пенал 1530», но total_width_mm всё равно сумма).
- **OCR-заземление размеров ПОЧИНЕНО:** Tesseract 5.4 установлен (winget, `C:\Program Files\Tesseract-OCR`) + добавлен
  в PATH; `pytesseract 0.3.13` в .venv; в `dim_ocr.py` добавлен `_configure_tesseract_cmd()` (явный путь к tesseract.exe).
  z.ai GLM-OCR всё ещё без средств (HTTP 429), но локальный Tesseract работает.
  В `image_analyzer.py` добавлено правило: если модель дала `total_width_mm > 8000` — доверяем крупному OCR-числу (≥1000мм).
  **ПРОВЕРЕНО: стр. 0002 → 9360 исправляется на 4860** (conf 0.69, но правило >8000 срабатывает).
- **Калибровка `select_total_width` СДЕЛАНА:** выбор теперь по ЗНАЧЕНИЮ, а не по позиции текста:
  (а) согласие с моделью — ближайшее OCR-число в ±10% (гейт `strength` убран); (б) иначе — КРУПНЕЙШЕЕ число,
  confidence по доминированию над вторым (исключая дубли по значению). Проверено: 0002 → 4860 (conf 0.95, 9360→4860);
  0004 → 1830 (conf 0.95, 3000→1830); 0011 → 1584≈1581 (conf 0.95, 2581→1584). Ветка «>8000» больше не обязательна,
  но оставлена как страховка.
